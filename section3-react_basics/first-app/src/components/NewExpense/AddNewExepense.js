const AddNewExepense = ({ show }) => {
 

  return (
    <div className="new-expense">
          <button onClick={show} type="sumbit">
            add expense
          </button>

    </div>
  );
};

export default AddNewExepense;
